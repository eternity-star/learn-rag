# 项目中遇到的问题

记录 learn-rag 开发与评测过程中的典型问题、原因与处理方式，便于复盘与后续迭代。

---

## 1. 按标题切分后，评测题 2 / 4 答「不知道」

### 现象

重建索引并改用 Markdown 标题切分后，对「配置仪器资源要配哪 4 项参数」「覆盖报告与添加至报告的区别」等问题，系统回答「根据现有资料，我不知道」。

### 原因

1. **切分过细或过粗都会伤检索**  
   - 过细（`###` / `####` 全拆开）：答案散落在低分片段。  
   - 仅按 `##` 切且节过长再按字数截断：含答案的片段语义分仍偏低（约 0.47～0.50）。
2. **纯向量检索在大语料下易被“近义无关”文档压过**  
   正确答案片段语义分低于「仪器选择 / 报告对比」等无关文档（约 0.52～0.60）。
3. **中文问题成整段匹配失败**  
   问题是「配置仪器资源时要配哪…」，正文只有「配置仪器资源」，整串匹配不上，关键词加分无效。

### 解决

| 手段 | 说明 |
|------|------|
| 切分策略 | 主切 `#` / `##`；超长节再按 `###` 细分；忽略空标题 |
| 简易 Hybrid | `score = 语义分 + 文件名加分 + 正文关键词加分` |
| 中文窗口 | 对长中文串再切 4～8 字窗口，提高命中率 |
| 阈值 / Top-K | `MIN_SCORE ≈ 0.45`，`topK = 5`，兼顾召回 |
| 修复短节 bug | 短节也统一为 `{ headings, content }`，避免 `piece.headings` 为 undefined |

复测后题 **2、4** 可正确回答，且引用落到《电生理接入报告配置教程》相关章节。

---

## 2. 题 14：通用采集卡菜单路径检错 / 胡编

### 现象

问「通用采集卡在 HO 客户端新版/老版从哪个菜单进参数设置」，命中 FTP、视频源等无关文档，或胡编菜单路径。

### 原因

1. 问题中的短词 **`HO`** 给大量文件名加分（source boost），干扰排序。  
2. 正确文档《采集卡设置------通用采集卡》语义分不够高，进不了 Top-K。

### 解决

- 英文 token 长度至少 **3**，避免 `ho` 误伤。  
- 问题含「采集卡」「电生理」时，对文件名做定向加分。  
- 配合正文关键词加分与切分优化。

复测后题 **14** 可正确回答。

---

## 3. 题 10：前半正确、后半塞入无关内容

### 现象

「什么是通用采集卡」核心答对，但后面跟出未提问的内容。

### 原因

- 固定字数切分时，片段带上后续无关段落。  
- Top-K 把多段一并塞进 prompt，模型容易“顺着写”。

### 解决

- 按标题切分，控制片段边界。  
- 检索加分让正确片段靠前；system 约束「只根据参考资料回答」。

复测后题 **10** 表现正常。

---

## 4. 拒答题（12 / 13）胡编价格、官网

### 现象

知识库没有价格/官网时，仍编造具体数字或下载地址。

### 原因

1. 中等相似度片段（约 0.58）仍通过 `MIN_SCORE`，进入 prompt。  
2. 模型在“有一点相关资料”时容易补全常识/猜测。  
3. 多轮对话上下文过长时，更容易串台。

### 解决

- system 增加：参考资料未直接写明**价格 / 下载地址 / 官网**时，必须答不知道，禁止猜测。  
- 适当提高或审视 `MIN_SCORE`；C 类题用**新会话**评测。  
- 拒答主要靠「无足够命中 + 强约束」，不能只靠阈值。

---

## 5. 重建索引报错：`Cannot read properties of undefined (reading 'length')`

### 现象

调用 `/api/rag/reindex` 或 `build-index` 时，在 `chunk.ts` 抛出：

```text
TypeError: Cannot read properties of undefined (reading 'length')
```

### 原因

短节分支写成了 `pieces = [body]`（**字符串**数组），后续却访问 `piece.headings.length`。

### 解决

短节与长节统一为：

```ts
{ headings: string[]; content: string }[]
```

修复后全库试切通过，再重建索引即可。

---

## 6. 环境与联调相关（早期）

### 6.1 `pnpm exec tsc --init` 失败与 `ERR_PNPM_IGNORED_BUILDS`

- **原因**：pnpm 默认忽略部分依赖的 build scripts（如 esbuild）。  
- **处理**：`pnpm approve-builds` 放行需要的包后重试。

### 6.2 PowerShell 请求中文 JSON 乱码 / 解析失败

- **原因**：PowerShell 对 `curl` / 请求体引号、编码处理不当。  
- **处理**：用 Postman，或 `ConvertTo-Json` / `--data-binary @body.json`；流式用 `curl.exe -N`。

### 6.3 中转站无 Embeddings（`model_not_found` / 无 channel）

- **原因**：聊天通道与 embedding 通道分离，`deepseek-embedding` 未开通。  
- **处理**：本地 Embedding（`@xenova/transformers` + `Xenova/bge-small-zh-v1.5`），Chat 仍走中转站。

---

## 7. 经验沉淀（简表）

| 原则 | 说明 |
|------|------|
| 先评测再优化 | 用 `rag-eval.md` 固定题目，对比改前改后 |
| 切分与检索要一起看 | 切对但检不出来 = 仍失败 |
| 大语料慎信纯向量 | 加文件名 / 正文关键词（Hybrid）往往立刻见效 |
| 中文匹配别指望整句 | 需要对长问句做子串/窗口 |
| 拒答与召回分开设计 | 阈值 + prompt + 新会话评测 |
| 改切分必重建索引 | 否则 `chunks.json` 仍是旧数据 |

---

## 相关文件

- 切分：`server/src/services/chunk.ts`
- 检索 / 索引：`server/src/services/indexer.ts`
- RAG 路由：`server/src/routes/rag.ts`
- 评测表：`rag-eval.md`
- 本地向量：`server/src/services/embedding.ts`
